# 📚 Catálogo de Livros (CRUD com React, TypeScript e JSON Server)

Este é um projeto de estudo que implementa as quatro operações básicas de um CRUD (Create, Read, Update, Delete) para gerenciamento de um catálogo de livros. O frontend é construído com **React** e **TypeScript**, e o backend (simulado) é feito com **JSON Server**.

## ✨ Funcionalidades

* **Visualização (Read):** Lista todos os livros cadastrados, exibindo título, autor, ano e ISBN.
* **Adicionar (Create):** Formulário para cadastrar novos livros na base de dados.
* **Editar (Update):** Funcionalidade para modificar os dados de um livro existente.
* **Excluir (Delete):** Remoção permanente de um livro da lista.
* **Estilização:** Layout responsivo e centralizado, focado na usabilidade.

## 🛠️ Tecnologias Utilizadas

* **Frontend:**
    * React.js
    * TypeScript
    * Vite (como *bundler* e ambiente de desenvolvimento)
    * CSS (Estilização nativa)
* **Backend/Mock API:**
    * JSON Server
    * `axios` (para requisições HTTP)

## 🚀 Como Executar o Projeto Localmente

Para rodar este projeto em sua máquina, siga os passos abaixo:

### 1. Clonar o Repositório

Abra o terminal e execute o comando:

```bash
git clone [https://github.com/ykarocardoso/catalogo-livros-react-ts.git](https://github.com/ykarocardoso/catalogo-livros-react-ts.git)
cd catalogo-livros-react-ts